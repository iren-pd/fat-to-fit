export enum ActivityLevel {
  Low = 'low',
  Medium = 'medium',
  High = 'high',
}

export enum GoalType {
  Lose = 'lose',
  Maintain = 'maintain',
  Gain = 'gain',
}

export enum Gender {
  Male = 'male',
  Female = 'female',
}
